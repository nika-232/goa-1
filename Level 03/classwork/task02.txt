text_info = "ეს არის ტექსტური ინფორმაცია Python პროგრამაში." number_count = 42 

განმარტება:

text_info – შეიცავს ტექსტურ ინფორმაციას (სტრინგს).

number_count – შეიცავს ციფრულ მნიშვნელობას (რიცხვს), მაგალითად 42
